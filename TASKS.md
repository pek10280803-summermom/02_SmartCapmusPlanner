# Smart Campus Planner - 단계별 작업 계획 (TASKS)

이 문서는 `PRD.md`에 정의된 요구사항과 MVP 범위를 바탕으로, 프로젝트를 한 번에 만들지 않고 **5단계로 나누어 순서대로 구현**하기 위한 작업 계획입니다.

각 단계는 이전 단계에서 만든 결과물 위에 기능을 추가하는 방식으로 진행되며, 비전공자도 이해할 수 있도록 "무엇을, 왜 만드는지" 위주로 설명합니다.

---

## Task 1. 기본 화면과 할 일 추가 기능 만들기

### 설명
앱의 첫 화면(기본 뼈대)을 만들고, 사용자가 "할 일 제목"을 입력해서 목록에 추가할 수 있는 가장 기본적인 기능을 구현합니다. 아직 카테고리나 중요도, 마감일 같은 세부 옵션은 넣지 않고, "할 일을 적고 목록에 쌓는다"는 핵심 동작만 먼저 완성합니다.

### 이 단계에서 만드는 것
- 할 일을 입력할 수 있는 입력창과 "추가" 버튼
- 추가한 할 일이 리스트 형태로 화면에 표시됨
- 여러 개의 할 일을 계속 추가할 수 있음

### 관련 PRD 요구사항
- F-01 할 일 추가

### 왜 먼저 하나요?
모든 기능(카테고리, 중요도, 완료율 등)은 결국 "할 일 목록"이 있어야 그 위에 붙일 수 있기 때문에, 가장 먼저 목록의 기본 골격을 만듭니다.

---

## Task 2. 할 일에 세부 정보(카테고리, 중요도, 마감일) 붙이기

### 설명
Task 1에서 만든 "할 일 추가" 기능을 확장해서, 할 일을 추가할 때 **카테고리(학과/동아리/개인)**, **중요도(상/중/하)**, **마감일**을 함께 입력할 수 있도록 만듭니다. 그리고 목록에 표시된 각 할 일 옆에 이 정보들이 눈에 띄게(색상, 라벨 등) 보이도록 합니다.

### 이 단계에서 만드는 것
- 할 일 추가 시 카테고리 선택(학과 / 동아리 / 개인)
- 할 일 추가 시 중요도 선택(상 / 중 / 하)
- 할 일 추가 시 마감일(날짜) 선택
- 목록에서 각 할 일의 카테고리·중요도·마감일이 한눈에 보이도록 표시

### 관련 PRD 요구사항
- F-02 카테고리 설정
- F-03 중요도 설정
- F-04 마감일 지정

### 왜 이 순서인가요?
할 일 자체를 추가하는 기본 기능(Task 1)이 먼저 완성되어 있어야, 그 위에 "부가 정보 입력 항목"을 자연스럽게 추가할 수 있습니다.

---

## Task 3. 완료 체크와 삭제 기능 만들기

### 설명
목록에 쌓인 할 일들을 관리할 수 있도록, 각 항목을 **완료 처리(체크)**하거나 **목록에서 삭제**할 수 있는 기능을 추가합니다. 완료된 할 일은 시각적으로 구분(예: 취소선, 흐리게 표시 등)되도록 합니다.

### 이 단계에서 만드는 것
- 할 일 목록의 각 항목에 체크박스(완료 표시) 추가
- 체크박스를 누르면 완료/미완료 상태가 바뀜
- 각 항목 옆에 삭제 버튼 추가, 누르면 목록에서 제거됨
- 완료된 항목은 스타일로 구분되어 보임

### 관련 PRD 요구사항
- F-05 완료 체크
- F-06 할 일 삭제

### 왜 이 순서인가요?
할 일 목록에 정보(카테고리, 중요도, 마감일)까지 다 들어간 뒤에, 그 항목들을 "관리"하는 기능(완료/삭제)을 붙이는 것이 자연스럽습니다.

---

## Task 4. 전체 완료율 표시 기능 만들기

### 설명
지금까지 등록된 전체 할 일 중에서 완료된 할 일이 몇 %인지 계산해서 화면 상단 등에 보여주는 기능을 추가합니다. 예를 들어 "완료율: 40%" 같은 형태로 표시하고, 할 일을 추가·완료·삭제할 때마다 이 숫자가 자동으로 업데이트되도록 합니다.

### 이 단계에서 만드는 것
- 전체 할 일 개수와 완료된 할 일 개수를 계산
- 완료율(%)을 화면에 표시하는 영역 추가
- 할 일 추가/완료 체크/삭제가 일어날 때마다 완료율이 실시간으로 갱신됨

### 관련 PRD 요구사항
- F-07 전체 완료율 표시

### 왜 이 순서인가요?
완료율은 "완료 체크" 기능(Task 3)이 있어야 계산할 수 있는 값이기 때문에, 완료 체크 기능이 완성된 다음 단계에서 진행합니다.

---

## Task 5. 새로고침해도 데이터 유지되도록 저장 기능 만들기

### 설명
지금까지는 화면을 새로고침하면 입력했던 할 일들이 모두 사라지는 상태입니다. 이 단계에서는 브라우저에 데이터를 저장해두어서, 새로고침을 하거나 나중에 다시 접속해도 이전에 등록한 할 일 목록·완료 상태·완료율이 그대로 남아있도록 만듭니다.

### 이 단계에서 만드는 것
- 할 일을 추가/수정/완료/삭제할 때마다 브라우저에 자동으로 저장
- 페이지를 새로고침하거나 다시 열었을 때, 저장된 할 일 목록을 불러와서 화면에 그대로 표시
- 완료 상태와 완료율도 함께 유지됨

### 관련 PRD 요구사항
- F-08 데이터 유지(저장)

### 왜 마지막 단계인가요?
저장 기능은 "무엇을 저장할지"가 먼저 정해져 있어야 의미가 있습니다. 할 일 목록, 카테고리/중요도/마감일, 완료 상태, 완료율까지 모든 기능이 갖춰진 뒤에, 이 전체 데이터를 저장하고 불러오는 기능을 마지막에 완성합니다.

---

## 전체 단계 요약

| 단계 | 핵심 목표 | 관련 요구사항 |
|------|-----------|----------------|
| Task 1 | 할 일 추가 및 목록 표시 기본 기능 | F-01 |
| Task 2 | 카테고리 / 중요도 / 마감일 입력 및 표시 | F-02, F-03, F-04 |
| Task 3 | 완료 체크 및 삭제 기능 | F-05, F-06 |
| Task 4 | 전체 완료율 계산 및 표시 | F-07 |
| Task 5 | 새로고침해도 유지되는 저장 기능 | F-08 |

이 순서대로 진행하면, 매 단계마다 실제로 눈으로 확인 가능한 기능이 하나씩 완성되며, 이전 단계의 결과 위에 자연스럽게 다음 기능을 쌓아 올릴 수 있습니다.
