# Smart Campus Planner - 단계별 구현 요청 프롬프트 (PROMPTS)

이 문서는 `PRD.md`(프로젝트 목적/요구사항/MVP)와 `TASKS.md`(Task 1~5 작업 순서)를 바탕으로, 실제로 AI(예: Cline)에게 **한 단계씩** 구현을 요청할 때 사용할 수 있는 프롬프트 모음입니다.

사용 방법:
1. Task 1 프롬프트를 먼저 사용해서 구현을 요청합니다.
2. 결과를 직접 실행해보고 확인합니다.
3. 문제가 없으면 Task 2 프롬프트로 넘어갑니다.
4. 이런 식으로 Task 5까지 순서대로 진행합니다.

---

## Task 1 프롬프트 - 기본 화면과 할 일 추가 기능

```
Smart Campus Planner 프로젝트의 Task 1을 구현해줘.

PRD.md와 TASKS.md 파일을 참고해서, 지금은 다음 기능만 만들어줘.

- 웹 브라우저에서 열 수 있는 간단한 할 일 관리 화면을 만들어줘.
- 화면에는 할 일 제목을 입력할 수 있는 입력창과 "추가" 버튼이 있어야 해.
- "추가" 버튼을 누르면 입력한 할 일이 아래쪽 목록에 나타나야 해.
- 여러 개의 할 일을 계속 추가할 수 있어야 해.

카테고리, 중요도, 마감일, 완료 체크, 삭제, 완료율, 새로고침 후 데이터 유지 기능은 이번 단계에서는 만들지 마.
아직은 "할 일을 적고 목록에 쌓이는 것"까지만 잘 동작하면 돼.

구현이 끝나면 내가 직접 화면에서 확인해볼 수 있도록, 실행 방법(예: 어떤 파일을 열면 되는지)도 알려줘.
```

---

## Task 2 프롬프트 - 카테고리 / 중요도 / 마감일 입력 및 표시

```
Task 1에서 만든 할 일 추가 기능을 이어서 확장해줘.

이번에는 할 일을 추가할 때 다음 정보도 함께 입력할 수 있게 만들어줘.

- 카테고리: 학과 / 동아리 / 개인 중 하나를 선택
- 중요도: 상 / 중 / 하 중 하나를 선택
- 마감일: 날짜를 선택할 수 있는 입력란

그리고 목록에 표시되는 각 할 일 옆에 이 카테고리, 중요도, 마감일이 눈에 잘 보이도록 표시해줘.
예를 들어 색깔이나 태그처럼 구분되면 좋겠어.

완료 체크, 삭제, 완료율, 새로고침 후 데이터 유지 기능은 아직 만들지 말고, 이번 단계 범위만 구현해줘.
기존에 Task 1에서 만든 "할 일 추가되고 목록에 쌓이는 기능"은 그대로 유지하면서 확장해줘.

구현이 끝나면 내가 직접 확인해볼 수 있도록, 어떻게 실행하고 확인하면 되는지 알려줘.
```

---

## Task 3 프롬프트 - 완료 체크와 삭제 기능

```
지금까지 만든 할 일 목록 기능에 이어서, 이번에는 다음 기능을 추가해줘.

- 목록에 있는 각 할 일 옆에 체크박스를 추가해서, 체크하면 "완료" 상태로 바뀌게 해줘.
- 완료된 할 일은 예를 들어 취소선이나 흐린 글씨 등으로 눈에 띄게 구분되도록 해줘.
- 각 할 일 옆에 삭제 버튼도 추가해서, 누르면 그 할 일이 목록에서 사라지게 해줘.

지금까지 만든 카테고리, 중요도, 마감일 표시 기능은 그대로 유지하면서 이 기능을 추가해줘.
완료율 표시와 새로고침 후 데이터 유지 기능은 아직 만들지 마.

구현이 끝나면 완료 체크와 삭제가 잘 되는지 내가 직접 확인해볼 수 있도록 실행 방법을 알려줘.
```

---

## Task 4 프롬프트 - 전체 완료율 표시

```
지금까지 만든 기능에 이어서, 전체 할 일 중 몇 %가 완료되었는지 화면에 보여주는 기능을 추가해줘.

- 예를 들어 화면 위쪽에 "완료율: 40%"처럼 표시되면 좋겠어.
- 할 일을 새로 추가하거나, 완료 체크를 하거나, 삭제할 때마다 이 완료율 숫자가 자동으로 다시 계산되어 바뀌어야 해.
- 계산 방법은 "완료된 할 일 개수 ÷ 전체 할 일 개수 × 100"으로 해줘.

기존에 만든 할 일 추가, 카테고리/중요도/마감일 표시, 완료 체크, 삭제 기능은 그대로 유지해줘.
새로고침 후 데이터 유지 기능은 아직 만들지 마.

구현이 끝나면 완료율이 상황에 맞게 잘 바뀌는지 내가 직접 확인해볼 수 있도록 실행 방법과 확인 방법을 알려줘.
```

---

## Task 5 프롬프트 - 새로고침해도 데이터 유지되는 저장 기능

```
지금까지 만든 기능에 이어서, 마지막으로 다음 기능을 추가해줘.

- 지금은 화면을 새로고침하면 입력했던 할 일들이 다 사라지는데, 새로고침을 하거나 브라우저를 다시 열어도 이전에 등록한 할 일 목록이 그대로 남아있도록 만들어줘.
- 할 일을 추가하거나, 완료 체크를 하거나, 삭제할 때마다 그 변경 내용이 자동으로 저장되어야 해.
- 완료 상태와 완료율도 새로고침 후에 그대로 유지되어야 해.

기존에 만든 할 일 추가, 카테고리/중요도/마감일, 완료 체크, 삭제, 완료율 기능은 모두 그대로 유지하면서 이 저장 기능만 추가해줘.

구현이 끝나면, 할 일을 몇 개 추가한 뒤 새로고침을 해봐도 목록이 그대로 남아있는지 내가 직접 확인해볼 수 있도록 확인 방법을 알려줘.
```

---

## 진행 방식 안내

- 각 프롬프트는 이전 단계에서 만든 결과를 유지한 채로 기능을 하나씩 추가하도록 작성되어 있습니다.
- 한 번에 하나의 프롬프트만 사용해서 요청하고, 결과를 직접 눈으로 확인한 뒤에 다음 프롬프트로 넘어가는 것을 권장합니다.
- 중간에 원하는 대로 동작하지 않는 부분이 있다면, 다음 단계로 넘어가기 전에 "지금 만든 기능에서 ○○ 부분이 이상해, 고쳐줘" 같은 방식으로 먼저 수정을 요청하면 됩니다.
